#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
JAR_FILE="$ROOT_DIR/target/helios-1.0-SNAPSHOT.jar"

if [[ ! -f "$JAR_FILE" ]]; then
  echo "Build not found. Run: mvn -q package"
  exit 1
fi

java -jar "$JAR_FILE" "$@"
